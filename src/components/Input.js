import {React} from "react";

const Input = ({value, handler, }) => {
    return(
        <div>
            <input type="text"/>
            <span>Your name</span>
        </div>
    )
}

export default Input